# Tools / 工具

Repository-maintenance helpers live here. Historical helper scripts are retained under [`legacy`](legacy) without being treated as part of a versioned MHD API.

仓库维护辅助脚本放在这里。早期工具原样保存在 [`legacy`](legacy)，不属于任何版本的 MHD 公开 API。
